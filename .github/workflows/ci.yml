name: CI

on:
  push:
    branches:
      - main

jobs:
  # build:
  #   name: Package Build 🪛
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v2

  #     - name: Use Node.js
  #       uses: actions/setup-node@v2
  #       with:
  #         node-version: '14.x'
      
  #     - name: Installing Packages 📝
  #       run: npm install

  #     - name: Building Application 🍏
  #       run: npm run build

  # unit_testing:
  #   needs: [build]
  #   name: Running units tests 🧪
  #   strategy:
  #     matrix:
  #       # os: [macos-latest, windows-latest, ubuntu-latest]
  #       # version: [12, 14, 16]

  #   runs-on: ${{ matrix.os }}

  #   steps:
  #     - uses: actions/checkout@v2

  #     - name: Use Node.js
  #       uses: actions/setup-node@v2
  #       with:
  #         node-version: ${{ matrix.version }}

  #     - name: Running tests
  #       run: |
  #         npm install
  #         npm test

  release:
    # needs: [unit_testing]
    name: Creating a new release 🎀
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'

      - name: Create Release ✒️
        id: release
        uses: halvardssm/github-action-tag-release@v1
        with:
          token: ${{ secrets.TOKEN_GITHUB }}
          path: "./package.json"

      -  run: |
          echo 'Release created: ${{steps.version.outputs.release_created}}'
          echo 'Release exists: ${{steps.version.outputs.release_exists}}'
          echo 'Release tag: ${{steps.version.outputs.release_tag}}'